[package]
name = "postgres-heartbeat"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
chrono = "0.4.26"
reqwest = { version = "0.11", features = ["blocking"] }
postgres = "0.19.5"

